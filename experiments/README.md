# Experiments

Only two experiment families are active:

1. `synthetic_known_pole/`: falsify the representation under known latent truth;
2. `real_paired_rate/`: added only after the synthetic gate passes.

The repository currently contains only the analytic contract. No learned or real-data result is claimed.
