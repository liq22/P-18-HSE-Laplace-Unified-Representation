# TII current execution: source-supported HSE–LLapDiff

The main question is cross-dataset partially observable latent posterior inference and its industrial diagnostic value. HSE provides an acquisition-aware structured condition and a statistical anchor; LLapDiff is the proposed core conditional generator on a source-qualified target. R/head_affine/M remain condition-interface ablations, not the whole paper.

## Current validated slice

The current Introduction/Related Work, role formulation, support/identifiability proofs, finite Notebook and editable Motivation Figure are built by:

```bash
bash paper/build_frontmatter.sh outputs/tii_support
```

This produces a cited manuscript preview, 14 finite theoretical checks and an executed Notebook, not new learned industrial evidence. The same 31 original works in `literature_matrix.md` enter the prose. Existing coordinate tests, native component results and MFPT reference remain unchanged and retain their original scope.

## Next substantive implementation

Use the existing industrial data/split path and an actual source-trained reference. First demonstrate common mechanical/latent block correspondence and determine the source-supported, jointly identifiable missing target without target labels. Then add the proposed projection to the actual LLapDiff forward/reverse path, verify it with a genuine paired-source batch and retain observed uncertainty. Do not claim an arbitrary VAE mask has physical meaning. A mask without conditional identification is not sufficient.

The existing native three-arm command is still useful for E3, but it does not implement the new source-restricted posterior. E2 compares deterministic/Gaussian/mixture/ordinary diffusion/LLapDiff on the same admitted target; E1 measures source-only industrial LODO. E4 includes no restriction, all-missing generation and reject-all controls. E5 separates coordinate changes from irrecoverable support loss.

Keep `experiments.md` as the scientific protocol and `results_native.md` as the evidence record. Existing `goals/` supply data/sync/GPU commands; the updated Goals02/04 below take precedence over earlier coordinate-only scientific descriptions. Do not add a second trainer, reader or goal registry. The local 8×4090 machine starts with one GPU; two-GPU training is prohibited. Normal tested dev merge only, preserving master, other branches and PHMFactory core.
